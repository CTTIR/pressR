% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout-index.R
\name{pr_layout_saddle_novel}
\alias{pr_layout_saddle_novel}
\title{Novel/Pliance 16x16 Saddle Mat Layout}
\usage{
pr_layout_saddle_novel(
  pressure_range = c(0, 63.75),
  sensor_area_cm2 = NA_real_,
  ...
)
}
\arguments{
\item{pressure_range}{Numeric vector of length 2. Default
\code{c(0, 63.75)} kPa, the hardware ceiling (255 x 0.25 kPa).}

\item{sensor_area_cm2}{Numeric. Cell area in cm²; \code{NA_real_} by default
because the manufacturer never published one.}

\item{...}{Further arguments passed to \code{\link[=pr_layout_from_index_map]{pr_layout_from_index_map()}}, e.g.
\code{name}, \code{spacing_mm}, \code{description}, \code{regions}.}
}
\value{
A \link{pr_layout} with 256 active sensors and a \code{channel_map} field.
}
\description{
The ready-made layout for the two-panel 16 x 16 (256 sensor) capacitive
saddle mat exported by Novel/Pliance systems. All 256 sensors are active.
}
\details{
The mat is built from two 16 x 8 panels. Device channel 1 sits at grid
(row 1, column 9) — the first cell of the \emph{right} panel — and channel 9
at (row 1, column 8), with each grid row advancing the channel number by
16 and the left panel running right-to-left. Applying
\code{pressure[, pr_channel_order(layout)]} to a raw export therefore places
every channel at its true anatomical position, which is what makes
\code{\link[=pr_mask_saddle_6]{pr_mask_saddle_6()}}, \code{\link[=pr_mask_symmetry]{pr_mask_symmetry()}}, \code{\link[=pr_calc_cop]{pr_calc_cop()}} and the
heatmaps simultaneously correct. Without the permutation the aggregate
statistics (mean, peak, total) are unchanged, so a wrong map is invisible
in every summary and wrong in every map.

No sensor pitch or cell area is published for this mat, so both default
to \code{NA} and the coordinates are grid indices — see the \emph{Missing physical
scale} section of \code{\link[=pr_layout_from_index_map]{pr_layout_from_index_map()}}.
}
\examples{
layout <- pr_layout_saddle_novel()
layout$n_sensors
layout$channel_map[1, 1:10]

# Device channel 1 lands at grid (row 1, col 9):
pos <- match(1L, pr_channel_order(layout))
layout$coords_mm[pos, c("row", "col")]
}
\seealso{
Other channel map functions:
\code{\link[=pr_channel_order]{pr_channel_order()}},
\code{\link[=pr_layout_from_index_map]{pr_layout_from_index_map()}}
}
\concept{channel map functions}
