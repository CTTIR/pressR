% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout-index.R
\name{pr_layout_from_index_map}
\alias{pr_layout_from_index_map}
\title{Build a Layout From an Explicit Channel Map}
\usage{
pr_layout_from_index_map(
  index_matrix,
  name = "custom",
  sensor_area_cm2 = NA_real_,
  spacing_mm = NA_real_,
  pressure_range = NULL,
  ...
)
}
\arguments{
\item{index_matrix}{Numeric matrix. \code{index_matrix[r, c]} is the device
channel number sitting at grid cell (\code{r}, \code{c}). Channel numbers must be
whole, positive and unique. \code{NA} or \code{0} marks a grid cell with no sensor
behind it (that cell becomes inactive). If every cell is active the
channel numbers must be exactly \code{1:length(index_matrix)}.}

\item{name}{Character. Short layout identifier. Default \code{"custom"}.}

\item{sensor_area_cm2}{Numeric. Area of one sensor cell in cm², or
\code{NA_real_} (the default) when the manufacturer never published one.
See \emph{Missing physical scale} below.}

\item{spacing_mm}{Numeric. Centre-to-centre sensor pitch in mm, or
\code{NA_real_} (the default) when it is unknown. See \emph{Missing physical
scale} below.}

\item{pressure_range}{Numeric vector of length 2, or \code{NULL} (default) for
\code{c(0, NA_real_)} — a device whose ceiling is not known.}

\item{...}{Further arguments passed to \code{\link[=pr_layout]{pr_layout()}}, e.g. \code{regions},
\code{description}, \code{manufacturer}, \code{model}, \code{pressure_unit}.}
}
\value{
A \link{pr_layout} object with three extra fields: \code{channel_map} (the
validated integer map, \code{NA} for empty cells), \code{channel_order} (the
permutation returned by \code{\link[=pr_channel_order]{pr_channel_order()}}) and \code{coords_units}.
}
\description{
Creates a \link{pr_layout} from a matrix that states, for every grid cell,
which \emph{device channel} is wired to it. This is the general answer to the
problem that a sensor mat's channel numbering rarely matches the order in
which the grid is read. The resulting layout carries the map with it, so
\code{\link[=pr_channel_order]{pr_channel_order()}} can hand a reader the one permutation that puts raw
device columns into the column-major grid order that \code{\link[=pr_mask]{pr_mask()}},
\code{\link[=pr_calc_cop]{pr_calc_cop()}} and the plotting functions already assume.
}
\section{Missing physical scale}{

\code{pr_layout()} requires a positive \code{sensor_area_cm2} and physical
\code{x_mm} / \code{y_mm} coordinates. Many real datasets have neither: the cell
area and the sensor pitch were simply never recorded. Rather than invent
a number, this function
\itemize{
\item builds the object through \code{\link[=pr_layout]{pr_layout()}} with a placeholder area and then
writes \code{NA_real_} into \code{sensor_area_cm2}, so the layout stays a valid
\code{pr_layout} and \code{\link[=pr_validate_layout]{pr_validate_layout()}} still passes, while anything
derived from area (force in N, contact area in cm²) honestly returns
\code{NA}; and
\item when \code{spacing_mm} is \code{NA}, lays the coordinates out on a unit grid
(\code{x_mm = col - 1}, \code{y_mm = row - 1}). Ordering, masks and centre of
pressure are then all correct, but \strong{the \code{x_mm} / \code{y_mm} columns are
grid-index offsets, not millimetres} — a COP of \code{x_mm = 8} means
column 9, not 8 mm. The layout records this in \verb{$coords_units}
(\code{"grid_index"} or \code{"mm"}) and keeps the requested pitch in
\verb{$spacing_mm}.
}

Supply a real \code{sensor_area_cm2} / \code{spacing_mm} and the layout behaves as
a fully physical one. Note that \code{\link[=plot.pr_layout]{plot.pr_layout()}} sizes region tiles from
\code{sensor_area_cm2}, so pass \code{regions} only alongside a real area.
}

\examples{
# A 2x2 mat whose channels are wired right-to-left within each row.
m <- rbind(c(2L, 1L), c(4L, 3L))
lay <- pr_layout_from_index_map(m, name = "toy_2x2")
lay$n_sensors
pr_channel_order(lay)

# Raw device frame: only channel 1 is loaded.
raw <- matrix(c(10, 0, 0, 0), nrow = 1)
grid_cols <- raw[, pr_channel_order(lay), drop = FALSE]
# Channel 1 is at grid (1, 2), i.e. the 3rd cell in column-major order.
which(grid_cols[1, ] > 0)
}
\seealso{
Other channel map functions:
\code{\link[=pr_channel_order]{pr_channel_order()}},
\code{\link[=pr_layout_saddle_novel]{pr_layout_saddle_novel()}}
}
\concept{channel map functions}
