% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frame-metrics.R
\name{pr_ref_pci}
\alias{pr_ref_pci}
\title{Pressure Concentration Index Interpretation Bands}
\usage{
pr_ref_pci()
}
\value{
A \link[tibble:tibble]{tibble::tibble} with columns \code{region}, \code{parameter},
\code{threshold}, \code{unit}, \code{interpretation}, \code{source}, \code{band}, \code{band_min},
\code{band_max}.
}
\description{
Working interpretation bands for the whole-grid PCI returned by
\code{\link[=pr_calc_pci]{pr_calc_pci()}}.
}
\details{
The first six columns match the other \verb{pr_ref_*} tables
(\code{\link[=pr_ref_saddle]{pr_ref_saddle()}}, \code{\link[=pr_ref_diabetic_foot]{pr_ref_diabetic_foot()}}, \code{\link[=pr_ref_wheelchair]{pr_ref_wheelchair()}}), with
\code{threshold} holding the lower edge of each band; \code{band}, \code{band_min} and
\code{band_max} are appended so the interval can be used programmatically.
\code{band_max} is \code{Inf} for the open top band.

Unlike the other reference tables, these bands are \strong{not} taken from a
published clinical threshold study. They are the working ranges used to
describe whole-grid saddle-mat recordings, and they apply only to
\code{pr_calc_pci(trial, denominator = "grid")}. PCI is dimensionless and
scale-free in pressure, but it is \emph{not} comparable across devices with
different grid sizes, because the whole-grid mean depends on how much
unloaded mat surrounds the contact patch.
}
\examples{
pr_ref_pci()
# Classify one recording:
pci <- pr_calc_pci(pr_example_trial("saddle_horse"))
ref <- pr_ref_pci()
ref$band[pci >= ref$band_min & pci < ref$band_max]
}
\seealso{
Other whole-grid frame metrics:
\code{\link[=pr_calc_cop_grid]{pr_calc_cop_grid()}},
\code{\link[=pr_calc_loaded_count]{pr_calc_loaded_count()}},
\code{\link[=pr_calc_mean_pressure_grid]{pr_calc_mean_pressure_grid()}},
\code{\link[=pr_calc_pci]{pr_calc_pci()}},
\code{\link[=pr_calc_total_pressure]{pr_calc_total_pressure()}},
\code{\link[=pr_frame_metrics]{pr_frame_metrics()}}
}
\concept{whole-grid frame metrics}
