% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset-cohort.R
\name{pr_dataset_filter}
\alias{pr_dataset_filter}
\title{Filter a Dataset by Trial Metadata, With Count Assertions}
\usage{
pr_dataset_filter(
  dataset,
  f,
  label = NULL,
  n_units = NULL,
  n_subjects = NULL,
  subject_field = "ID"
)
}
\arguments{
\item{dataset}{A \link{pr_dataset} object, or a list of \link{pr_trial} objects.}

\item{f}{A predicate applied to each trial's \code{metadata} list. Either a
function of one argument, or a one-sided formula such as
\code{~ .x$Mode == "MS"}. It must return a single \code{TRUE}/\code{FALSE} per trial;
\code{NA} is treated as \code{FALSE} with a warning.}

\item{label}{Character. Short name for this filter, stored on the result
as \code{filter_label} and appended to \code{filter_history}, and used in
assertion messages. Default \code{NULL} uses \code{"filter"}.}

\item{n_units}{Integer. Expected number of trials after filtering, or
\code{NULL} (default) for no assertion.}

\item{n_subjects}{Integer. Expected number of distinct subjects after
filtering, or \code{NULL} (default) for no assertion.}

\item{subject_field}{Character. Metadata field identifying the subject.
Default \code{"ID"}.}
}
\value{
A \link{pr_dataset} containing the surviving trials, with extra
elements \code{filter_label} and \code{filter_history}.
}
\description{
Keeps the trials whose metadata satisfy a predicate, records the filter
label on the returned dataset, and — when \code{n_units} or \code{n_subjects} are
given — asserts that the surviving cohort has exactly the expected size.
The assertion is the point: a cohort step that cannot fail cannot protect
a result.
}
\examples{
layout <- pr_layout_mat("16")
mk <- function(id, mode) {
  pr_trial(matrix(1, 3, layout$n_sensors), time = c(0, 0.02, 0.04),
           layout = layout, metadata = list(ID = id, Mode = mode))
}
ds <- pr_dataset(list(mk("ID001", "MS"), mk("ID001", "MH"),
                      mk("ID002", "MS")))
walk <- pr_dataset_filter(ds, ~ .x$Mode == "MS", label = "walk",
                          n_units = 2, n_subjects = 2)
walk$filter_label
}
\seealso{
Other cohort functions:
\code{\link[=pr_assert_cohort]{pr_assert_cohort()}},
\code{\link[=pr_design_table]{pr_design_table()}},
\code{\link[=pr_validate_dataset]{pr_validate_dataset()}}
}
\concept{cohort functions}
