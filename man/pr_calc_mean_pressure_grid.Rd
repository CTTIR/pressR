% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frame-metrics.R
\name{pr_calc_mean_pressure_grid}
\alias{pr_calc_mean_pressure_grid}
\title{Whole-Grid Mean Pressure Per Frame}
\usage{
pr_calc_mean_pressure_grid(trial)
}
\arguments{
\item{trial}{A \link{pr_trial} object.}
}
\value{
Numeric vector of length \code{n_frames}, in the trial's pressure unit.
}
\description{
Mean pressure across the entire sensor grid for each frame: the frame sum
divided by the number of sensors, with unloaded cells counted as zeros.
}
\details{
This is deliberately \emph{not} the same quantity as \code{\link[=pr_calc_mean_pressure]{pr_calc_mean_pressure()}},
which averages only the cells above \code{threshold} (loaded-cells-only, by
definition) and is left untouched. On a saddle mat where a typical frame
loads roughly 4\% of the 256 cells, the loaded-cells-only mean is around
25 times the whole-grid mean, so the two are never interchangeable. Use
this one when the mat itself is the region of interest and an unloaded
cell is a real "no pressure here" measurement; use
\code{\link[=pr_calc_mean_pressure]{pr_calc_mean_pressure()}} when the sensor sheet is larger than the loaded
object and only the contact patch is meaningful.

The denominator is \code{trial$layout$n_sensors}, i.e. the number of \emph{active}
sensors, which equals \code{ncol(trial$pressure)}. Inactive grid cells are not
measurements and never enter the average.
}
\examples{
trial <- pr_example_trial("saddle_horse")
grid_mean <- pr_calc_mean_pressure_grid(trial)
loaded_mean <- pr_calc_mean_pressure(trial)
# The whole-grid mean is the smaller of the two by construction:
all(grid_mean <= loaded_mean + 1e-9)
}
\seealso{
Other whole-grid frame metrics:
\code{\link[=pr_calc_cop_grid]{pr_calc_cop_grid()}},
\code{\link[=pr_calc_loaded_count]{pr_calc_loaded_count()}},
\code{\link[=pr_calc_pci]{pr_calc_pci()}},
\code{\link[=pr_calc_total_pressure]{pr_calc_total_pressure()}},
\code{\link[=pr_frame_metrics]{pr_frame_metrics()}},
\code{\link[=pr_ref_pci]{pr_ref_pci()}}
}
\concept{whole-grid frame metrics}
