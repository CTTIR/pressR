% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-pliance.R
\name{pr_meta_from_filename}
\alias{pr_meta_from_filename}
\title{Split Metadata Fields Out of a File Name}
\usage{
pr_meta_from_filename(
  path,
  fields,
  sep = "_",
  on_fail = c("skip", "error", "na")
)
}
\arguments{
\item{path}{Character scalar. A file path or bare name. The directory and
the extension are removed before splitting.}

\item{fields}{Character vector of output column names, in the order the
parts appear in the name.}

\item{sep}{Character scalar. Literal separator between parts.
Default \code{"_"}.}

\item{on_fail}{What to do when the name does not split into exactly
\code{length(fields)} non-empty parts: \code{"skip"} (default) returns \code{NULL},
\code{"error"} throws, \code{"na"} returns a one-row tibble of \code{NA_character_}.}
}
\value{
A one-row tibble with one character column per entry of \code{fields},
or \code{NULL} (see \code{on_fail}).
}
\description{
Many measurement archives encode a recording's design cells in its file
name, e.g. \code{ID052_K_K_KG00_MS.asc} for subject \code{ID052}, saddle \code{K},
pad \code{K}, weight \code{KG00}, mode \code{MS}. This splits such a base name into a
one-row tibble of named fields.
}
\details{
A name matches only when it splits into \emph{exactly} \code{length(fields)}
non-empty parts. That strictness is the point: it is what lets a
directory read reject stray files (repeat takes such as
\code{ID003_K_K_KG60_MH_R01}, notes, exports) instead of silently mislabelling
them.
}
\examples{
fields <- c("ID", "Saddle", "Pad", "Weight", "Mode")
pr_meta_from_filename("ID052_K_K_KG00_MS.asc", fields)

# A repeat take carries a sixth part, so it does not match:
pr_meta_from_filename("ID003_K_K_KG60_MH_R01.asc", fields)

pr_meta_from_filename("ID003_K_K_KG60_MH_R01.asc", fields, on_fail = "na")
}
\seealso{
Other Pliance ingest functions:
\code{\link[=pr_read_dir]{pr_read_dir()}},
\code{\link[=pr_read_pliance]{pr_read_pliance()}}
}
\concept{Pliance ingest functions}
