% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensor-map.R
\name{pr_batch_frame_summary}
\alias{pr_batch_frame_summary}
\title{Per-Recording Frame Summary for a Whole Cohort}
\usage{
pr_batch_frame_summary(
  dataset,
  threshold = 0,
  meta_fields = NULL,
  .progress = FALSE
)
}
\arguments{
\item{dataset}{A \link{pr_dataset} object, or a list of \link{pr_trial} objects.}

\item{threshold}{Numeric. Cells at or below this value count as unloaded;
passed to \code{\link[=pr_frame_metrics]{pr_frame_metrics()}}, where it affects \code{loaded} and the COP
weighting only. Default \code{0}.}

\item{meta_fields}{Character vector of metadata field names to prepend,
in the order given. \code{NULL} (default) auto-detects: every field that
holds a single value in every trial, dropping those that are \code{NA}
throughout. Pass \code{character(0)} for the nine statistics alone.}

\item{.progress}{Logical. Show a progress bar over trials. Default
\code{FALSE}.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per trial.
}
\description{
Runs \code{\link[=pr_frame_metrics]{pr_frame_metrics()}} over every trial in a dataset and reduces each
frame table to a single row: the nine summary statistics a cohort
analysis reports per recording, plus the trial's metadata.
}
\details{
Columns, in order: the requested metadata fields, then
\itemize{
\item \code{mean_kPa_avg} — mean over frames of the whole-grid frame mean.
\item \code{mean_kPa_sd} — standard deviation over frames of that same quantity,
i.e. how much overall load varied during the recording (\code{NA} for a
single-frame trial).
\item \code{peak_kPa_max} — the largest single-sensor reading anywhere in the
recording. On a saturating device this pins to the hardware ceiling.
\item \code{peak_kPa_avg} — mean over frames of the frame maximum. Together with
\code{mean_kPa_avg} this gives the concentration index
\code{peak_kPa_avg / mean_kPa_avg} (see \code{\link[=pr_calc_pci]{pr_calc_pci()}}).
\item \code{total_kPa_avg} — mean over frames of the frame sum. Exactly
\code{mean_kPa_avg * n_sensors}; a sum of pressures, never a force.
\item \code{loaded_avg} — mean over frames of the loaded cell count.
\item \code{cop_row_mean}, \code{cop_col_mean} — mean over frames of the centre of
pressure, in grid index units.
\item \code{n_frames} — frames in the recording, after any the reader dropped.
}

These names and definitions are the cohort contract, chosen so the result
can be compared column-for-column against a study's own summary table.
Note that \code{mean_kPa_avg} is the \emph{whole-grid} mean including unloaded
cells (see \code{\link[=pr_calc_mean_pressure_grid]{pr_calc_mean_pressure_grid()}}), which is a different quantity
from \code{\link[=pr_calc_mean_pressure]{pr_calc_mean_pressure()}}.
}
\section{Memory}{

The frame tables are built and discarded one at a time, so peak memory is
one recording's frames, not the cohort's. That matters: 431 recordings of
5.5 M frames total would need several gigabytes if every frame table were
materialised first, to produce nine numbers each.
}

\examples{
ds <- pr_dataset(list(
  pr_example_trial("saddle_horse", seed = 1),
  pr_example_trial("saddle_horse", seed = 2)
))
fs <- pr_batch_frame_summary(ds, meta_fields = character(0))
names(fs)
round(fs$mean_kPa_avg, 3)

# total_kPa_avg is mean_kPa_avg scaled by the sensor count:
n_sensors <- ds$trials[[1]]$layout$n_sensors
all.equal(fs$total_kPa_avg, fs$mean_kPa_avg * n_sensors)
}
\seealso{
\code{\link[=pr_frame_metrics]{pr_frame_metrics()}} for the per-frame table this summarises,
\code{\link[=pr_design_table]{pr_design_table()}} for the metadata side alone.

Other sensor aggregation functions:
\code{\link[=pr_profile_matrix]{pr_profile_matrix()}},
\code{\link[=pr_sensor_map]{pr_sensor_map()}}
}
\concept{sensor aggregation functions}
