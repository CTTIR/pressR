% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset-cohort.R
\name{pr_assert_cohort}
\alias{pr_assert_cohort}
\title{Assert Cohort Size}
\usage{
pr_assert_cohort(
  x,
  n_units = NULL,
  n_subjects = NULL,
  unit_col = NULL,
  subject_col = NULL,
  label = NULL
)
}
\arguments{
\item{x}{A \link{pr_dataset} object, a list of \link{pr_trial} objects, or a data
frame (for example the output of \code{\link[=pr_design_table]{pr_design_table()}}).}

\item{n_units}{Integer. Expected number of units, or \code{NULL} to skip.
A unit is a trial (dataset) or a row (data frame) unless \code{unit_col}
names a field/column, in which case units are its distinct values.}

\item{n_subjects}{Integer. Expected number of distinct subjects, or
\code{NULL} to skip.}

\item{unit_col}{Character. Field/column whose distinct values are the
units. \code{NULL} (default) counts trials or rows.}

\item{subject_col}{Character. Field/column identifying the subject.
\code{NULL} (default) uses the first of \code{"subject_id"} or \code{"ID"} that
exists, and aborts if neither does.}

\item{label}{Character. Name of the cohort, used in messages. \code{NULL}
(default) uses the dataset's \code{filter_label} or \code{name}, else
\code{"cohort"}.}
}
\value{
Invisibly returns \code{x}, so the assertion can sit in a pipeline.
Aborts with condition class \code{pr_cohort_assert_failed} on mismatch.
}
\description{
Checks that a cohort holds exactly the expected number of units (trials
or rows) and, optionally, of distinct subjects. Aborts with a message
naming the expectation, the actual count, and the shortfall or surplus.
}
\details{
Use it as a tripwire around every step that can silently drop data —
reading a directory, filtering, joining. A guard that has never fired
proves nothing, so give it the real numbers from your design.
}
\examples{
design <- data.frame(
  ID = c("ID001", "ID001", "ID002"),
  Mode = c("MS", "MH", "MS")
)
pr_assert_cohort(design, n_units = 3, n_subjects = 2, subject_col = "ID")

# A wrong expectation fails loudly:
try(pr_assert_cohort(design, n_units = 431, label = "all recordings"))
}
\seealso{
Other cohort functions:
\code{\link[=pr_dataset_filter]{pr_dataset_filter()}},
\code{\link[=pr_design_table]{pr_design_table()}},
\code{\link[=pr_validate_dataset]{pr_validate_dataset()}}
}
\concept{cohort functions}
