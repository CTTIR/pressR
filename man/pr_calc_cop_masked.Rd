% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regional-symmetry.R
\name{pr_calc_cop_masked}
\alias{pr_calc_cop_masked}
\title{Centre of Pressure Per Region}
\usage{
pr_calc_cop_masked(trial, masks = NULL, threshold = 0, units = c("grid", "mm"))
}
\arguments{
\item{trial}{A \link{pr_trial} object.}

\item{masks}{Named list of \link{pr_mask} objects or logical matrices. \code{NULL}
(default) uses the layout's own regions.}

\item{threshold}{Numeric. Cells at or below this value are given zero
weight. Default \code{0}.}

\item{units}{Character. \code{"grid"} (default) or \code{"mm"}; see \emph{Units}.}
}
\value{
A named list of \link{pr_cop} objects, one per mask, in the order the
masks were given.
}
\description{
Computes one centre-of-pressure trajectory for each region mask, weighted
only by that region's sensors. Returns a list of \link{pr_cop} objects, so the
existing trajectory vocabulary — path length, mean and maximum velocity,
the x/y ranges \code{\link[=pr_calc_cop_excursion]{pr_calc_cop_excursion()}} reports, and the 95\% ellipse sway
area — is available per region with no further work.
}
\details{
A whole-mat COP is a single point that can sit in an unloaded gap between
two loaded zones, and it moves whenever load shifts \emph{between} zones. A
per-region COP cannot: it stays inside its own region and reports how load
migrated within it, which is what separates a saddle that rocks
fore-and-aft from one that slides sideways.

Frames in which a region carries no load at all get \code{NA} for both axes,
exactly as \code{\link[=pr_calc_cop]{pr_calc_cop()}} does for the whole mat. \code{\link[=pr_cop]{pr_cop()}} excludes
those frames from the trajectory metrics, so a region that is loaded only
intermittently still reports a meaningful path length — though one
measured across the gaps, not through them.
}
\section{Units}{

\code{units = "grid"} (the default) returns grid index units: \code{x} is the
fractional sensor column, \code{y} the fractional sensor row, both 1-based, in
the convention of \code{\link[=pr_calc_cop_grid]{pr_calc_cop_grid()}}. This is always available.

\code{units = "mm"} returns the layout's physical \code{x_mm} / \code{y_mm} coordinates,
as \code{\link[=pr_calc_cop]{pr_calc_cop()}} does. It is refused for a layout whose coordinates are
grid indices because no sensor pitch was ever published (see the \emph{Missing
physical scale} section of \code{\link[=pr_layout_from_index_map]{pr_layout_from_index_map()}}) — millimetres
would be fabricated, and a path length in fabricated millimetres is worse
than none.

Note that a \code{pr_cop} object labels its fields \code{mm} regardless; under
\code{units = "grid"} they are grid indices, and the derived path length and
velocity are in index units and index units per second.
}

\examples{
trial <- pr_example_trial("saddle_horse")
cops <- pr_calc_cop_masked(trial)
names(cops)

# Each region's COP stays inside its own rows:
round(vapply(cops, function(c) mean(c$y, na.rm = TRUE), numeric(1)), 2)

# The full pr_cop vocabulary works unchanged:
round(vapply(cops, function(c) c$path_length, numeric(1)), 3)

# This layout has a real sensor pitch, so millimetres are honest:
round(pr_calc_cop_masked(trial, units = "mm")$cranial_left$range_x, 1)
}
\seealso{
\code{\link[=pr_calc_cop]{pr_calc_cop()}} and \code{\link[=pr_calc_cop_grid]{pr_calc_cop_grid()}} for the whole mat,
\code{\link[=pr_calc_cop_excursion]{pr_calc_cop_excursion()}} for the range summary.

Other regional symmetry functions:
\code{\link[=pr_calc_regional_map]{pr_calc_regional_map()}},
\code{\link[=pr_calc_symmetry_map]{pr_calc_symmetry_map()}},
\code{\link[=pr_mask_mirror_balance]{pr_mask_mirror_balance()}},
\code{\link[=pr_mask_rowbands]{pr_mask_rowbands()}},
\code{\link[=pr_symmetry_sensitivity]{pr_symmetry_sensitivity()}}
}
\concept{regional symmetry functions}
