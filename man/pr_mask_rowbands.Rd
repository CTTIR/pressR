% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regional-symmetry.R
\name{pr_mask_rowbands}
\alias{pr_mask_rowbands}
\title{Row-Band Region Masks With an Explicit Split}
\usage{
pr_mask_rowbands(
  layout,
  breaks = c(5, 11),
  sides = TRUE,
  band_names = c("cranial", "middle", "caudal")
)
}
\arguments{
\item{layout}{A \link{pr_layout} object.}

\item{breaks}{Numeric vector of \code{length(band_names) - 1} strictly
increasing whole numbers in \code{1:(grid_rows - 1)}. Each is the last row of
its band. Default \code{c(5, 11)}.}

\item{sides}{Logical. Cross each band with the left and right halves of
the mat. Default \code{TRUE}.}

\item{band_names}{Character vector of band names, cranial to caudal.
Default \code{c("cranial", "middle", "caudal")}.}
}
\value{
A named list of \link{pr_mask} objects: \code{length(band_names)} of them
when \code{sides = FALSE}, twice that when \code{sides = TRUE}.
}
\description{
Builds cranial / middle / caudal row bands (optionally crossed with the
left and right halves of the mat) from \strong{stated} split rows rather than
from a derived \code{floor(grid_rows / 3)}. The split is the argument, so a
study's own band definition survives into the code instead of being
approximated by a division.
}
\details{
\code{breaks} gives the \strong{last row of each band except the last}. With the
default \code{c(5, 11)} on a 16-row mat the bands are rows 1:5, 6:11 and 12:16
— the split used by \code{\link[=pr_layout_saddle]{pr_layout_saddle()}}'s \code{regions}, in which the middle
band is one row deeper than the outer two.

\code{\link[=pr_mask_saddle_6]{pr_mask_saddle_6()}} uses \code{third <- floor(16 / 3)}, which gives 1:5, 6:10,
11:16 instead: the \emph{caudal} band is the deep one. Passing
\code{breaks = c(5, 10)} reproduces those cells exactly, with the same names in
the same order, so the two conventions can be compared rather than
confused. Because that one-row difference moves eight sensors between two
zones, it is worth stating which convention a number came from.

Left and right are split at \code{floor(grid_cols / 2)}, matching
\code{\link[=pr_mask_symmetry]{pr_mask_symmetry()}} and \code{\link[=pr_mask_saddle_6]{pr_mask_saddle_6()}}. With \code{sides = TRUE} the
masks come back in band-major order (\code{cranial_left}, \code{cranial_right},
\code{middle_left}, ...); with \code{sides = FALSE} there is one full-width mask per
band. Every mask is intersected with \code{layout$active}, so inactive cells
(a withers cutout, say) never enter a band.
}
\examples{
layout <- pr_layout_saddle("horse")
masks <- pr_mask_rowbands(layout)
names(masks)
vapply(masks, function(m) m$n_sensors, integer(1))

# breaks = c(5, 10) is the pr_mask_saddle_6() convention:
same <- pr_mask_rowbands(layout, breaks = c(5, 10))
identical(same$middle_left$matrix, pr_mask_saddle_6(layout)$middle_left$matrix)

# Full-width bands, no left/right split:
names(pr_mask_rowbands(layout, sides = FALSE))
}
\seealso{
\code{\link[=pr_mask_saddle_6]{pr_mask_saddle_6()}} for the derived-thirds convention,
\code{\link[=pr_calc_regional_map]{pr_calc_regional_map()}} which consumes these masks.

Other regional symmetry functions:
\code{\link[=pr_calc_cop_masked]{pr_calc_cop_masked()}},
\code{\link[=pr_calc_regional_map]{pr_calc_regional_map()}},
\code{\link[=pr_calc_symmetry_map]{pr_calc_symmetry_map()}},
\code{\link[=pr_mask_mirror_balance]{pr_mask_mirror_balance()}},
\code{\link[=pr_symmetry_sensitivity]{pr_symmetry_sensitivity()}}
}
\concept{regional symmetry functions}
