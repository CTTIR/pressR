% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout-index.R
\name{pr_channel_order}
\alias{pr_channel_order}
\title{Raw Device Channel Order for a Layout}
\usage{
pr_channel_order(layout_or_map)
}
\arguments{
\item{layout_or_map}{A \link{pr_layout} built by
\code{\link[=pr_layout_from_index_map]{pr_layout_from_index_map()}} (or \code{\link[=pr_layout_saddle_novel]{pr_layout_saddle_novel()}}), a channel
index matrix as accepted by \code{\link[=pr_layout_from_index_map]{pr_layout_from_index_map()}}, or an integer
vector that already is such a permutation.}
}
\value{
An integer vector of length \code{layout$n_sensors}.
}
\description{
Returns the integer permutation that reorders the columns of a raw device
export into the column-major grid order every other \code{pressR} function
assumes. A reader applies it once:
\code{pressure <- raw[, pr_channel_order(layout), drop = FALSE]}.
}
\details{
Element \code{k} of the result is the \emph{device channel} that belongs in pressure
column \code{k}, where column \code{k} corresponds to \code{which(layout$active)[k]} —
the same column-major ordering used by \code{\link[=pr_mask]{pr_mask()}} and
\code{.layout_build_coords()}.
}
\examples{
pr_channel_order(rbind(c(2L, 1L), c(4L, 3L)))

ord <- pr_channel_order(pr_layout_saddle_novel())
length(ord)
ord[1:8]
}
\seealso{
Other channel map functions:
\code{\link[=pr_layout_from_index_map]{pr_layout_from_index_map()}},
\code{\link[=pr_layout_saddle_novel]{pr_layout_saddle_novel()}}
}
\concept{channel map functions}
