% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{pr_design_gaps}
\alias{pr_design_gaps}
\title{Observed Design Cells Against the Full Crossing}
\usage{
pr_design_gaps(x, factors, expected = NULL)
}
\arguments{
\item{x}{A data frame.}

\item{factors}{Character vector of column names to cross, in the order
they should nest.}

\item{expected}{Single non-negative whole number: the replicate count a
complete cell should hold. \code{NULL} (default) uses the modal non-zero
observed count.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per cell of the full crossing:
the factor columns (as character), then \code{n} (rows observed),
\code{expected}, \code{delta} (\code{n - expected}) and \code{status}, one of \code{"missing"}
(\code{n == 0}), \code{"under"}, \code{"ok"} or \code{"over"}.
}
\description{
Crosses the levels of the named factors, counts how many rows fall in
each cell, and labels every cell of the crossing as missing,
under-represented, balanced, or over-represented. This is what surfaces a
nesting or aliasing problem before anything is modelled.
}
\details{
A design described as "all saddles crossed with all pads" can turn out to
be nothing of the sort: one pad may only ever have been used with one
saddle, in which case the two factors are aliased and their effects are
not separately estimable — a fact no model summary will state out loud.
Counting the cells of the full crossing shows it immediately: a factor
nested inside another leaves whole blocks of the crossing at \code{n == 0}.

Levels come from \code{levels()} for a factor column and from the sorted
unique non-\code{NA} values otherwise. Sorting uses \code{method = "radix"}, which
forces C-locale collation, so the row order of the result does not depend
on the machine's locale. The crossing is generated by
\code{\link[tidyr:expand_grid]{tidyr::expand_grid()}}, so the first factor varies slowest and the row
order is the natural nesting order.

\code{expected} sets the replicate count a balanced cell should hold. Left
\code{NULL}, it is the most common non-zero cell count — the design's own idea
of a full cell — with ties broken towards the larger count, so a tie errs
towards reporting cells as under-represented rather than as balanced.

Rows with \code{NA} in any crossing factor cannot be placed in a cell. They
are dropped and a warning names how many, because dropping them silently
would understate the gaps.
}
\examples{
design <- data.frame(
  Saddle = c("K", "K", "K", "S", "S", "W"),
  Pad = c("F", "K", "K", "F", "F", "W")
)
gaps <- pr_design_gaps(design, c("Saddle", "Pad"))
gaps

# Pad is nested within Saddle, not crossed with it:
sum(gaps$status == "missing")

# With an explicit replicate expectation:
pr_design_gaps(design, c("Saddle", "Pad"), expected = 2)
}
\seealso{
\code{\link[=pr_factor_contract]{pr_factor_contract()}} for the level sets being crossed,
\code{\link[=pr_design_table]{pr_design_table()}} for building \code{x} from a \link{pr_dataset}.

Other provenance functions:
\code{\link[=pr_factor_contract]{pr_factor_contract()}},
\code{\link[=pr_lock_table]{pr_lock_table()}},
\code{\link[=pr_validate_summary]{pr_validate_summary()}},
\code{\link[=pr_verify_lock]{pr_verify_lock()}}
}
\concept{provenance functions}
