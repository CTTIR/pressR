% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensor-quality.R
\name{pr_calc_gradient}
\alias{pr_calc_gradient}
\title{Spatial Pressure Gradient Magnitude}
\usage{
pr_calc_gradient(
  trial,
  statistic = "mean",
  method = "central",
  edges = c("na", "zero"),
  threshold = 0
)
}
\arguments{
\item{trial}{A \link{pr_trial} object.}

\item{statistic}{Character. The per-sensor map to differentiate; any
statistic accepted by \code{\link[=pr_sensor_map]{pr_sensor_map()}}. Default \code{"mean"}, which is the
cohort definition.}

\item{method}{Character. \code{"central"} (default) or \code{"forward"}; see
\emph{Details}.}

\item{edges}{Character. \code{"na"} (default) or \code{"zero"}; see \emph{The edge
ring}.}

\item{threshold}{Numeric. Cells at or below this value count as unloaded;
passed to \code{\link[=pr_sensor_map]{pr_sensor_map()}}. Default \code{0}.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per sensor, in the same
column-major grid order as \code{\link[=pr_sensor_map]{pr_sensor_map()}}, and columns \code{sensor}
(integer), \code{row}, \code{col} (integer grid indices) and \code{gradient}
(numeric).
}
\description{
How sharply pressure changes from one sensor to its neighbours. The
per-sensor map produced by \code{\link[=pr_sensor_map]{pr_sensor_map()}} is arranged back onto the
device grid, differenced in both directions, and returned as one gradient
magnitude per sensor.
}
\details{
For a grid \code{M} of per-sensor values, \code{method = "central"} computes
\deqn{g_{r,c} = \sqrt{\left(\frac{M_{r+1,c}-M_{r-1,c}}{2}\right)^2 +
  \left(\frac{M_{r,c+1}-M_{r,c-1}}{2}\right)^2}}
and \code{method = "forward"} uses the one-sided differences
\code{M[r+1, c] - M[r, c]} and \code{M[r, c+1] - M[r, c]} instead. Central
differences are symmetric and less noisy; forward differences reach one
cell further into the edge, which matters on a small grid.

The result is in the trial's pressure unit \strong{per sensor spacing} — kPa
per cell step, not kPa/mm. Converting to kPa/mm needs a published cell
pitch, and the saddle mat has none (see the \emph{Missing physical scale}
section of \code{\link[=pr_layout_from_index_map]{pr_layout_from_index_map()}}), so no distance scaling is done
anywhere in this function. Gradients are therefore comparable across
recordings on the same device and not across devices with different
pitches.
}
\section{The edge ring}{

A central difference needs a neighbour on both sides, so it is undefined
on the outermost row and column. The two \code{edges} settings differ only
there, and the interior is bit-for-bit identical either way:
\itemize{
\item \code{edges = "na"} (default) leaves the ring \code{NA}. This is the honest
answer: no gradient was computed for those cells.
\item \code{edges = "zero"} fills the ring with \code{0}. This reproduces the cohort
analysis, which wrote central differences for rows and columns
\code{2:(n-1)} into a zero-initialised matrix and never revisited the
border. On a 16x16 mat that makes 60 of 256 cells — nearly a quarter —
a \strong{structural zero} rather than a measurement. Use it to reproduce
published numbers, but never average, rank or threshold over the whole
grid with it: the mean gradient is biased down by a factor of roughly
\code{196/256}, and "the lowest-gradient cells" will be the border, every
time. \code{edges = "na"} with \code{na.rm = TRUE} gives the interior mean the
zero-filled version was probably meant to be.
}

Cells the layout marks inactive are \code{NA} in the grid, so a gradient that
would have had to read one comes back \code{NA} under either setting.
}

\examples{
trial <- pr_example_trial("saddle_horse")
g <- pr_calc_gradient(trial)
g[which.max(g$gradient), ]

# The default leaves the border -- and the layout's inactive gullet
# cells -- missing ...
sum(is.na(g$gradient))
# ... while the cohort's zero-filled convention hides the border:
gz <- pr_calc_gradient(trial, edges = "zero")
sum(gz$gradient == 0, na.rm = TRUE)

# Same interior, different whole-grid mean:
c(interior = mean(g$gradient, na.rm = TRUE),
  filled = mean(gz$gradient, na.rm = TRUE))
}
\seealso{
\code{\link[=pr_sensor_map]{pr_sensor_map()}} for the map being differentiated.

Other sensor quality functions:
\code{\link[=pr_sensor_quality]{pr_sensor_quality()}}
}
\concept{sensor quality functions}
