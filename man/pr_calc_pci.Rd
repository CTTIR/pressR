% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frame-metrics.R
\name{pr_calc_pci}
\alias{pr_calc_pci}
\title{Peak/Mean Pressure Concentration Index}
\usage{
pr_calc_pci(trial, denominator = c("grid", "loaded"), eps = 1e-12)
}
\arguments{
\item{trial}{A \link{pr_trial} object.}

\item{denominator}{Character. \code{"grid"} (default) or \code{"loaded"}; see
Details.}

\item{eps}{Numeric. Added to the mean-pressure denominator. Default
\code{1e-12}.}
}
\value{
A single numeric value (dimensionless ratio).
}
\description{
Ratio of a recording's average frame peak to its average frame mean. A
PCI of 1 would mean a perfectly uniform mat; the larger the value, the
more of the load sits under a single hotspot.
}
\details{
\code{PCI = mean(peak_kPa) / mean(mean_kPa)} — a ratio of two per-recording
averages, not the average of a per-frame ratio; the two differ whenever
load varies over time, and the cohort summaries use the former.

\code{denominator} selects which mean goes underneath:
\itemize{
\item \code{"grid"} (default) uses \code{\link[=pr_calc_mean_pressure_grid]{pr_calc_mean_pressure_grid()}}, the whole-grid
mean including unloaded cells. This is the cohort definition and the
one whose interpretation bands \code{\link[=pr_ref_pci]{pr_ref_pci()}} documents.
\item \code{"loaded"} uses the loaded-cells-only mean of \code{\link[=pr_calc_mean_pressure]{pr_calc_mean_pressure()}},
which is a different quantity — that function is loaded-cells-only by
definition and is untouched here. Values come out roughly an order of
magnitude smaller on a sparsely loaded mat, and \code{\link[=pr_ref_pci]{pr_ref_pci()}} does
\strong{not} apply to them.
}

\code{eps} guards the denominator so a recording with no load at all returns
\code{0} instead of \code{NaN}.
}
\examples{
trial <- pr_example_trial("saddle_horse")
pr_calc_pci(trial)
# The loaded-cells-only denominator is larger, so the index is smaller:
pr_calc_pci(trial, denominator = "loaded") < pr_calc_pci(trial)
}
\seealso{
Other whole-grid frame metrics:
\code{\link[=pr_calc_cop_grid]{pr_calc_cop_grid()}},
\code{\link[=pr_calc_loaded_count]{pr_calc_loaded_count()}},
\code{\link[=pr_calc_mean_pressure_grid]{pr_calc_mean_pressure_grid()}},
\code{\link[=pr_calc_total_pressure]{pr_calc_total_pressure()}},
\code{\link[=pr_frame_metrics]{pr_frame_metrics()}},
\code{\link[=pr_ref_pci]{pr_ref_pci()}}
}
\concept{whole-grid frame metrics}
