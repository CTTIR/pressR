% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frame-metrics.R
\name{pr_calc_cop_grid}
\alias{pr_calc_cop_grid}
\title{Center of Pressure in Grid Index Units}
\usage{
pr_calc_cop_grid(trial, threshold = 0, eps = 1e-12)
}
\arguments{
\item{trial}{A \link{pr_trial} object.}

\item{threshold}{Numeric. Cells at or below this value count as unloaded.
Default \code{0}.}

\item{eps}{Numeric. Added to the weight sum to keep unloaded frames finite.
Default \code{1e-12}.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per frame and columns \code{frame}
(integer), \code{time_s}, \code{cop_row}, \code{cop_col} (grid index units).
}
\description{
Pressure-weighted centroid of each frame expressed in grid indices: a
\code{cop_row} of 1 is the first sensor row, a \code{cop_col} of 16 the sixteenth
sensor column. Row and col are read from the layout's coordinate table
positionally, so the result stays correct under any device channel
permutation applied by the reader.
}
\details{
\code{\link[=pr_calc_cop]{pr_calc_cop()}} returns the same centroid in millimetres and gives \code{NA}
for frames with no load. This function is the index-unit counterpart used
when a device's physical sensor pitch was never published, so millimetre
coordinates would be fabricated. Instead of \code{NA}, an unloaded frame gets a
centroid of approximately \code{0} for both axes: \code{eps} is added to the weight
sum, so the quotient is \code{0 / eps} rather than \code{0 / 0}. That keeps the
output a complete, non-\code{NA} time series, and \code{cop_row == 0} is
unambiguous because a real centroid is always at least 1.

Note also that the whole-grid metrics here differ from
\code{\link[=pr_calc_mean_pressure]{pr_calc_mean_pressure()}}, which is loaded-cells-only by definition.
}
\examples{
trial <- pr_example_trial("saddle_horse")
cop <- pr_calc_cop_grid(trial)
range(cop$cop_row)
}
\seealso{
Other whole-grid frame metrics:
\code{\link[=pr_calc_loaded_count]{pr_calc_loaded_count()}},
\code{\link[=pr_calc_mean_pressure_grid]{pr_calc_mean_pressure_grid()}},
\code{\link[=pr_calc_pci]{pr_calc_pci()}},
\code{\link[=pr_calc_total_pressure]{pr_calc_total_pressure()}},
\code{\link[=pr_frame_metrics]{pr_frame_metrics()}},
\code{\link[=pr_ref_pci]{pr_ref_pci()}}
}
\concept{whole-grid frame metrics}
