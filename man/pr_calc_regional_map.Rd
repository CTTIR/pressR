% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regional-symmetry.R
\name{pr_calc_regional_map}
\alias{pr_calc_regional_map}
\title{Zone Statistics of the Time-Averaged Sensor Map}
\usage{
pr_calc_regional_map(
  trial,
  masks = NULL,
  statistic = c("mean", "max", "loaded"),
  threshold = 0
)
}
\arguments{
\item{trial}{A \link{pr_trial} object.}

\item{masks}{Named list of \link{pr_mask} objects or logical matrices. \code{NULL}
(default) uses the layout's own regions; a layout without regions is an
error, since a zone table has to say which zones.}

\item{statistic}{Character. The per-sensor reduction over time:
\code{"mean"} (default) the time-average, \code{"max"} the maximum pressure
picture, or \code{"loaded"} the fraction of frames above \code{threshold}.}

\item{threshold}{Numeric. Cells at or below this value count as unloaded.
Default \code{0}.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per mask and columns \code{zone},
\code{zone_mean_kPa}, \code{zone_peak_kPa}, \code{zone_loaded}.
}
\description{
Summarises each region mask on the \strong{per-sensor map}: every sensor is
first reduced over its own frames, and the zone statistics are then taken
across the sensors of the zone.
}
\section{Operator order}{

This is the opposite order to \code{\link[=pr_calc_regional]{pr_calc_regional()}}, and the two give
different numbers on purpose.
\itemize{
\item \code{\link[=pr_calc_regional]{pr_calc_regional()}} reduces \strong{over sensors within a frame}, then over
frames. Its \code{mpp} is the largest single reading the zone ever produced,
in any frame.
\item \code{pr_calc_regional_map()} reduces \strong{over frames within a sensor}, then
over sensors. Its \code{zone_peak_kPa} is the largest \emph{time-averaged} cell,
which is smaller — often several-fold — because no cell holds its
maximum for a whole recording.
}

Neither is more correct; they answer different questions. Use
\code{\link[=pr_calc_regional]{pr_calc_regional()}} to report what the worst moment looked like, and this
function to report which cells carry load over the ride, which is the
quantity a cohort-level zone table compares between horses.
}

\section{Columns}{

Writing \code{v} for the per-sensor map and \code{Z} for a zone's sensors:
\itemize{
\item \code{zone_mean_kPa} — \code{mean(v[Z])}, over \strong{all} the zone's sensors, with
never-loaded ones contributing zeros. The mat is the region of interest,
so an unloaded cell is a real measurement (compare
\code{\link[=pr_calc_mean_pressure_grid]{pr_calc_mean_pressure_grid()}}).
\item \code{zone_peak_kPa} — \code{max(v[Z])}.
\item \code{zone_loaded} — \code{sum(v[Z] > 0)}, the number of the zone's sensors that
ever carried load above \code{threshold}.
}

A zone with no sensors at all returns three zeros rather than \code{NaN}.

With \code{statistic = "loaded"} the map is a duty cycle in \verb{[0, 1]}, not a
pressure, and the first two columns carry fractions; the column names are
kept fixed so a cohort table has one schema.
}

\examples{
trial <- pr_example_trial("saddle_horse")
pr_calc_regional_map(trial)

# The sensor-first peak is never above the frame-first peak:
zm <- pr_calc_regional_map(trial)
zf <- pr_calc_regional(trial, parameters = "mpp")
all(zm$zone_peak_kPa <= zf$mpp + 1e-9)

# An explicit band split, rather than the layout's own regions:
pr_calc_regional_map(trial, pr_mask_rowbands(trial$layout, sides = FALSE))
}
\seealso{
\code{\link[=pr_sensor_map]{pr_sensor_map()}} for the map being reduced,
\code{\link[=pr_calc_regional]{pr_calc_regional()}} for the frame-first counterpart.

Other regional symmetry functions:
\code{\link[=pr_calc_cop_masked]{pr_calc_cop_masked()}},
\code{\link[=pr_calc_symmetry_map]{pr_calc_symmetry_map()}},
\code{\link[=pr_mask_mirror_balance]{pr_mask_mirror_balance()}},
\code{\link[=pr_mask_rowbands]{pr_mask_rowbands()}},
\code{\link[=pr_symmetry_sensitivity]{pr_symmetry_sensitivity()}}
}
\concept{regional symmetry functions}
