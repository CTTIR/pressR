% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{pr_validate_summary}
\alias{pr_validate_summary}
\title{Two-Tier Data Frame Validation}
\usage{
pr_validate_summary(x, hard = list(), soft = list(), label = NULL)
}
\arguments{
\item{x}{A data frame to validate.}

\item{hard}{Named list of predicates whose failure aborts. Default
\code{list()}.}

\item{soft}{Named list of predicates whose failure warns. Default
\code{list()}.}

\item{label}{Character. Name of the table, used in the warning and error
headers. \code{NULL} (default) uses \code{"data"}.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per rule and columns \code{rule},
\code{level} (\code{"hard"} or \code{"soft"}), \code{passed} (logical) and \code{message}
(\code{NA} for a passing rule), hard rules first. When a hard rule fails,
aborts with condition class \code{pr_validate_failed}, carrying that same
tibble in the condition's \code{results} field. Soft failures warn with
condition class \code{pr_validate_soft_failed}.
}
\description{
Runs a set of named predicates over a data frame and returns them as a
tibble of results. Rules passed as \code{hard} abort on failure; rules passed
as \code{soft} warn. Everything is evaluated before anything is signalled, so
one failing rule never hides the others.
}
\details{
The two tiers encode a distinction most validation code loses: some
properties are structural (a duplicated key, a negative duration, an
unknown condition code) and make every downstream number wrong, while
others are worth knowing about but not worth stopping for (an unbalanced
cell, an unusually short recording). Collapsing both into \code{\link[=stopifnot]{stopifnot()}}
means either the pipeline halts for cosmetic reasons or the structural
checks get commented out.

A rule is a function of the data frame returning either:
\itemize{
\item a logical vector — \code{TRUE} throughout passes; any \code{FALSE} or \code{NA} fails,
and the message counts the failing elements. This lets a rule be
written per row (\code{function(d) d$n_frames > 0}) or as a single claim
(\code{function(d) anyDuplicated(d$ID) == 0L}).
\item a character vector — an empty one passes, and any strings are taken as
the problem description. Use this when the rule can say \emph{what} is
wrong, not merely that something is.
}

A rule that throws is recorded as a failure carrying its error message
rather than propagated: a validation run should report on a broken rule,
not die inside it.
}
\examples{
design <- data.frame(
  ID = c("ID001", "ID001", "ID003"),
  Mode = c("MH", "MS", "MG"),
  n_frames = c(23432L, 10662L, 8000L)
)

res <- pr_validate_summary(
  design,
  hard = list(
    positive_frames = function(d) d$n_frames > 0,
    mode_known = function(d) all(d$Mode \%in\% c("MG", "MH", "MS"))
  ),
  label = "design"
)
res

# A soft rule warns and is still reported. The real cohort is unbalanced
# across Mode (92 / 169 / 170 recordings), which is worth knowing but is
# not a reason to stop.
unbalanced <- rbind(design, design[2, ])
res2 <- suppressWarnings(pr_validate_summary(
  unbalanced,
  soft = list(balanced = function(d) {
    tab <- table(d$Mode)
    if (length(unique(as.integer(tab))) == 1L) {
      character(0)
    } else {
      paste0("Mode counts differ: ",
             paste(names(tab), as.integer(tab), sep = "=",
                   collapse = ", "))
    }
  }),
  label = "design"
))
res2$message

# A hard failure aborts:
try(pr_validate_summary(
  design,
  hard = list(unique_id = function(d) anyDuplicated(d$ID) == 0L)
))
}
\seealso{
\code{\link[=pr_validate_dataset]{pr_validate_dataset()}} for the trial-level homogeneity checks.

Other provenance functions:
\code{\link[=pr_design_gaps]{pr_design_gaps()}},
\code{\link[=pr_factor_contract]{pr_factor_contract()}},
\code{\link[=pr_lock_table]{pr_lock_table()}},
\code{\link[=pr_verify_lock]{pr_verify_lock()}}
}
\concept{provenance functions}
