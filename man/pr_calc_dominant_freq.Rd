% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{pr_calc_dominant_freq}
\alias{pr_calc_dominant_freq}
\title{Dominant Oscillation Frequency in a Band}
\usage{
pr_calc_dominant_freq(
  trial,
  band = c(0.3, 5),
  signal = "total",
  window = "hann"
)
}
\arguments{
\item{trial}{A \link{pr_trial} object.}

\item{band}{Numeric vector of length 2. Inclusive search band in Hz.
Default \code{c(0.3, 5)}.}

\item{signal}{Character. Frame-level signal to transform. Default
\code{"total"}.}

\item{window}{Character. Window to taper with. Default \code{"hann"}.}
}
\value{
A one-row \link[tibble:tibble]{tibble::tibble} with columns \code{stride_freq_Hz} and
\code{peak_power}. Both are \code{NA} (with a warning) when the band contains no
frequency bin.
}
\description{
The strongest frequency of \code{\link[=pr_calc_spectrum]{pr_calc_spectrum()}} inside a search band —
on a saddle recording, the stride frequency.
}
\details{
The default band of 0.3-5 Hz brackets equine stride rates from a slow
walk to a fast canter while excluding both the baseline drift below it
and sensor noise above it. Band edges are inclusive, and the search
happens on the exact frequency axis documented in \code{\link[=pr_calc_spectrum]{pr_calc_spectrum()}}.

The result is the single largest bin, with no interpolation between
bins, so its resolution is \code{fs / n_frames} — 0.058 Hz for a 869-frame
recording at 50 Hz. A recording with no oscillation still returns its
largest in-band bin; read \code{peak_power} before trusting the frequency,
and compare against \code{\link[=pr_calc_stride_cycles]{pr_calc_stride_cycles()}}, which measures the same
rhythm in the time domain and disagrees when there is nothing periodic
to find.
}
\examples{
trial <- pr_example_trial("saddle_horse")
pr_calc_dominant_freq(trial)

# A stride frequency and a mean stride duration are reciprocals:
f <- pr_calc_dominant_freq(trial)$stride_freq_Hz
cycles <- pr_calc_stride_cycles(trial)
round(c(from_spectrum = 1 / f, from_cycles = mean(cycles$duration_s)), 2)
}
\seealso{
\code{\link[=pr_calc_spectrum]{pr_calc_spectrum()}} for the full spectrum.

Other temporal structure functions:
\code{\link[=pr_calc_phase_map]{pr_calc_phase_map()}},
\code{\link[=pr_calc_spectrum]{pr_calc_spectrum()}},
\code{\link[=pr_calc_stride_cycles]{pr_calc_stride_cycles()}},
\code{\link[=pr_cop_shape]{pr_cop_shape()}}
}
\concept{temporal structure functions}
