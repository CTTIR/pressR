% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensor-map.R
\name{pr_sensor_map}
\alias{pr_sensor_map}
\title{Per-Sensor Statistics Over Time}
\usage{
pr_sensor_map(
  trial,
  statistic = c("mean", "max", "sd", "pti", "pct_zero"),
  threshold = 0
)
}
\arguments{
\item{trial}{A \link{pr_trial} object.}

\item{statistic}{Character. One of \code{"mean"}, \code{"max"}, \code{"sd"}, \code{"pti"} or
\code{"pct_zero"}. See \emph{Details}.}

\item{threshold}{Numeric. Cells at or below this value count as unloaded.
Default \code{0}.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per sensor and columns \code{sensor}
(integer), \code{row}, \code{col} (integer grid indices) and \code{value} (numeric).
}
\description{
Reduces a recording along the time axis: one number per sensor, with the
sensor's grid coordinates attached. This is the counterpart of
\code{\link[=pr_frame_metrics]{pr_frame_metrics()}}, which reduces along the sensor axis instead.
}
\details{
The available statistics, for a sensor read at every frame:
\itemize{
\item \code{"mean"} — mean pressure, unloaded frames included in the denominator.
\item \code{"max"} — the maximum pressure picture (MPP): the peak this sensor ever
saw. \code{pr_sensor_map(trial, "max")$value} is the map
\code{\link[=pr_plot_heatmap]{pr_plot_heatmap()}} draws.
\item \code{"sd"} — standard deviation over frames (\code{NA} for a single-frame
trial), i.e. how much this sensor's load fluctuated.
\item \code{"pti"} — pressure-time integral, trapezoidal over the trial's own
timestamps; identical to \code{\link[=pr_calc_pti]{pr_calc_pti()}} at the default threshold.
\item \code{"pct_zero"} — fraction of frames in which the sensor was at or below
\code{threshold}. \code{1} means never loaded, \code{0} means always loaded; it is the
per-sensor duty cycle, and \code{1 - pct_zero} is the fraction of the
recording the sensor was in contact.
}

\code{threshold} treats cells at or below it as unloaded, exactly as in
\code{\link[=pr_frame_metrics]{pr_frame_metrics()}}: they are zeroed before \code{mean}, \code{sd} and \code{pti} are
taken, they cannot raise \code{max}, and they are what \code{pct_zero} counts. At
the default \code{threshold = 0} on non-negative data no masking is needed and
none is done.

The \code{sensor} column is the \emph{pressure column index}, taken positionally
from \code{trial$layout$coords_mm}, not the device channel number. For a
layout built by \code{\link[=pr_layout_from_index_map]{pr_layout_from_index_map()}} the two differ, and
\code{pr_channel_order(trial$layout)[sensor]} recovers the device channel.
Rows come back in that same column-major grid order, so \verb{$value} can be
used as a feature vector directly.
}
\examples{
trial <- pr_example_trial("saddle_horse")
mpp <- pr_sensor_map(trial, "max")
mpp[which.max(mpp$value), ]

# The mean map is never above the peak map, sensor for sensor:
avg <- pr_sensor_map(trial, "mean")
all(avg$value <= mpp$value + 1e-9)

# Fraction of the recording each sensor spent in contact:
duty <- 1 - pr_sensor_map(trial, "pct_zero")$value
round(range(duty), 3)
}
\seealso{
\code{\link[=pr_frame_metrics]{pr_frame_metrics()}} for the per-frame reduction,
\code{\link[=pr_profile_matrix]{pr_profile_matrix()}} to stack this across a dataset.

Other sensor aggregation functions:
\code{\link[=pr_batch_frame_summary]{pr_batch_frame_summary()}},
\code{\link[=pr_profile_matrix]{pr_profile_matrix()}}
}
\concept{sensor aggregation functions}
