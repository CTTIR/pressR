% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensor-map.R
\name{pr_profile_matrix}
\alias{pr_profile_matrix}
\title{Per-Sensor Feature Matrix for a Dataset}
\usage{
pr_profile_matrix(dataset, statistic = "mean", threshold = 0)
}
\arguments{
\item{dataset}{A \link{pr_dataset} object, or a list of \link{pr_trial} objects.}

\item{statistic}{Character. Any statistic accepted by \code{\link[=pr_sensor_map]{pr_sensor_map()}}.
Default \code{"mean"}.}

\item{threshold}{Numeric. Cells at or below this value count as unloaded.
Default \code{0}.}
}
\value{
A numeric matrix with \code{length(dataset)} rows and \code{n_sensors}
columns, with row and column names as described above.
}
\description{
Stacks \code{\link[=pr_sensor_map]{pr_sensor_map()}} across every trial in a dataset: a
\verb{n_trials x n_sensors} numeric matrix whose rows are recordings and whose
columns are sensors, in layout (column-major grid) order. This is the
input \code{stats::prcomp()}, \code{stats::dist()}, \code{stats::cmdscale()} or a
classifier expects, with no further reshaping.
}
\details{
Row names are the trials' identifying metadata (\code{trial_id}, else
\code{file_label}, else \code{subject_id}, else \verb{trial_<i>}), made unique with
\code{\link[=make.unique]{make.unique()}}. Column names are \verb{sensor_<id>} taken from the first
trial's layout.

Every trial must expose the same number of sensors, since column \code{k} has
to mean the same grid cell in every row; a mismatch is an error rather
than a recycled row. Differing \emph{layout names} at the same sensor count
are only a warning: two layouts may legitimately share a grid, but if
they do not, the matrix silently compares different anatomy, so the
warning is worth reading.
}
\examples{
ds <- pr_dataset(lapply(1:3, function(s) {
  pr_example_trial("saddle_horse", seed = s)
}))
X <- pr_profile_matrix(ds, "mean")
dim(X)
X[, 1:4]

# Straight into a PCA of pressure distribution shape:
pc <- stats::prcomp(X)
round(pc$sdev[1:2], 3)
}
\seealso{
\code{\link[=pr_sensor_map]{pr_sensor_map()}} for a single trial.

Other sensor aggregation functions:
\code{\link[=pr_batch_frame_summary]{pr_batch_frame_summary()}},
\code{\link[=pr_sensor_map]{pr_sensor_map()}}
}
\concept{sensor aggregation functions}
