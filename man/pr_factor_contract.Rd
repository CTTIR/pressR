% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.R
\name{pr_factor_contract}
\alias{pr_factor_contract}
\title{Assert the Level Sets of Design Factors}
\usage{
pr_factor_contract(x, levels_list, id_cols = NULL)
}
\arguments{
\item{x}{A data frame.}

\item{levels_list}{Named list. Each name is a column of \code{x}; each element
is the vector of levels that column must carry, in order. A \code{factor}
may be supplied instead, in which case its \code{levels()} are used. Include
\code{NA} to permit missing values in that column.}

\item{id_cols}{Character vector of column names identifying a row, used
to point at the offending rows in the error message (for example
\code{"ID"}). \code{NULL} (default) reports values without row identifiers.}
}
\value{
Invisibly, \code{x} — so the contract can sit inside a pipeline. On
violation, aborts with condition class \code{pr_factor_contract_failed},
carrying a \code{problems} \link[tibble:tibble]{tibble::tibble} (columns \code{column}, \code{issue},
\code{value}, \code{n_rows}, \code{ids}) in the condition.
}
\description{
Checks that named columns of a data frame carry exactly the levels they
are contracted to carry — no unexpected value, no silently absent level,
and for \code{factor} columns the declared order as well. Aborts naming the
offending column, the offending values, and the rows they sit in.
}
\details{
This is the check that catches a typo'd condition code, a level lost to a
filter, and a \code{factor()} call whose \verb{levels =} argument drifted out of
step with the data — all of which change a model's reference level or its
contrast matrix without changing anything visible in a \code{head()}.

Both directions are enforced, because "exactly" is the point: a value in
the data that the contract does not list is an \code{unexpected_level}, and a
level in the contract that never appears in the data is a
\code{missing_level}. The second half is the one that matters after a subset —
a design that lost a whole condition still looks perfectly well formed.

Order is enforced only where order exists. A \code{factor} column stores its
levels, so \code{levels(x[[col]])} must equal the contract element by element;
a mismatch in order alone is reported as \code{wrong_order}. A \code{character}
column has no stored order, so only its level \emph{set} is contracted —
convert with \code{factor(x, levels = ...)} first if the order has to be
pinned.

\code{NA} values are reported as \code{missing_value} unless \code{NA} appears in the
contract for that column, in which case they are accepted.
}
\examples{
design <- data.frame(
  ID = c("ID001", "ID001", "ID003", "ID003"),
  Mode = c("MG", "MH", "MS", "MH"),
  Saddle = c("K", "S", "W", "K")
)

# Passes and returns the data invisibly:
out <- pr_factor_contract(
  design,
  list(Mode = c("MG", "MH", "MS"), Saddle = c("K", "S", "W")),
  id_cols = "ID"
)
identical(out, design)

# A wrong contract fails loudly, naming the column and the values:
try(pr_factor_contract(design, list(Mode = c("MG", "MH"))))
}
\seealso{
\code{\link[=pr_design_gaps]{pr_design_gaps()}} for the combinations those levels do and do
not form.

Other provenance functions:
\code{\link[=pr_design_gaps]{pr_design_gaps()}},
\code{\link[=pr_lock_table]{pr_lock_table()}},
\code{\link[=pr_validate_summary]{pr_validate_summary()}},
\code{\link[=pr_verify_lock]{pr_verify_lock()}}
}
\concept{provenance functions}
