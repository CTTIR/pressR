% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset-cohort.R
\name{pr_validate_dataset}
\alias{pr_validate_dataset}
\title{Validate Cohort Homogeneity}
\usage{
pr_validate_dataset(dataset, strict = TRUE)
}
\arguments{
\item{dataset}{A \link{pr_dataset} object, or a list of \link{pr_trial} objects.}

\item{strict}{Logical. If \code{TRUE} (default), abort when any problem is
found. If \code{FALSE}, warn and return the problem table.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per problem and columns
\code{trial} (integer index), \code{trial_label}, \code{check}, \code{expected}, \code{actual}
and \code{problem}. Zero rows means the cohort is homogeneous. When
\code{strict = TRUE} and problems exist, aborts with condition class
\code{pr_dataset_invalid}, carrying the same tibble in the condition's
\code{problems} field.
}
\description{
Checks that every trial in a dataset is analysable together: same sensor
count, same layout name, non-empty, and free of missing, infinite or
negative pressure values. Anything that would make a cohort-level
summary meaningless is reported per trial.
}
\details{
The reference sensor count and layout name are the \emph{modal} values across
the dataset (ties broken by first appearance), so a single odd recording
is flagged rather than the other 430.
}
\examples{
layout <- pr_layout_mat("16")
mk <- function() {
  pr_trial(matrix(1, 3, layout$n_sensors), time = c(0, 0.02, 0.04),
           layout = layout)
}
pr_validate_dataset(pr_dataset(list(mk(), mk())))

# A mixed-layout cohort is reported, not silently summarised:
odd <- pr_example_trial("insole")
problems <- pr_validate_dataset(pr_dataset(list(mk(), mk(), odd)),
                                strict = FALSE)
problems$check
}
\seealso{
Other cohort functions:
\code{\link[=pr_assert_cohort]{pr_assert_cohort()}},
\code{\link[=pr_dataset_filter]{pr_dataset_filter()}},
\code{\link[=pr_design_table]{pr_design_table()}}
}
\concept{cohort functions}
