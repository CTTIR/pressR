% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-pliance.R
\name{pr_read_dir}
\alias{pr_read_dir}
\title{Read a Directory of Pressure Exports}
\usage{
pr_read_dir(
  dir,
  pattern = "\\\\.asc$",
  reader = pr_read_pliance,
  layout = NULL,
  meta_fields = NULL,
  recursive = TRUE,
  on_error = c("skip", "abort"),
  quiet = FALSE,
  ...,
  sep = "_",
  name = NULL
)
}
\arguments{
\item{dir}{Character scalar. Directory to walk.}

\item{pattern}{Regular expression matching file names to read. Default
\code{"\\\\.asc$"}.}

\item{reader}{Function called as \code{reader(path, layout = layout, ...)}.
Default \code{\link[=pr_read_pliance]{pr_read_pliance()}}.}

\item{layout}{A \link{pr_layout} passed to \code{reader}. Default \code{NULL}.}

\item{meta_fields}{Character vector of file-name fields, or \code{NULL}
(default) to read every matching file and parse no names.}

\item{recursive}{Logical. Descend into sub-directories. Default \code{TRUE}.}

\item{on_error}{\code{"skip"} (default) records a failure and continues;
\code{"abort"} re-throws the first one.}

\item{quiet}{Logical. Suppress the progress and summary messages and the
readers' warnings. Default \code{FALSE}.}

\item{...}{Further arguments passed to \code{reader}.}

\item{sep}{Character scalar. Separator passed to
\code{\link[=pr_meta_from_filename]{pr_meta_from_filename()}}. Default \code{"_"}.}

\item{name}{Character scalar. Dataset name. \code{NULL} (default) uses the
directory's base name.}
}
\value{
A \link{pr_dataset}. Attribute \code{"read_report"} holds a tibble with one
row per candidate file (\code{file}, \code{path}, \code{status}, \code{reason}).
}
\description{
Walks a directory, reads every matching file with \code{reader}, and collects
the results into a \link{pr_dataset}. Files that cannot be read - or whose
names do not match \code{meta_fields} - are reported and skipped rather than
aborting the whole batch.
}
\details{
When \code{meta_fields} is supplied, each file name is split with
\code{\link[=pr_meta_from_filename]{pr_meta_from_filename()}} and the parts are stored in the trial's
metadata under those names. The first part additionally fills
\code{subject_id} and the remaining parts, rejoined with \code{sep}, fill
\code{condition} - the field \code{\link[=pr_dataset]{pr_dataset()}} groups by - unless the reader
already set them. A name that does not split into exactly
\code{length(meta_fields)} parts is skipped, which is the intended way to
exclude repeat takes and stray exports from a cohort.
}
\examples{
dir <- tempfile()
dir.create(dir)
hdr <- c(
  "Dateiname:  demo.mat\tDatum/Zeit: 01.01.25 09.00",
  "Kalibrationsdatei:  demo_cal",
  "gesamte Messzeit [Sek.]: 0.040\tZeit pro Bild [Sek.]:  0.02000\tMessfrequenz [Hz]:   50",
  "Druckwerte in  kPa", "", "elektrisch:",
  "Zeit [Sek.]\t1\t2\t3\t4",
  "0.02000\t10\t0\t0\t0",
  "0.04000\t0\t20\t0\t0"
)
writeLines(hdr, file.path(dir, "ID001_K_KG00.asc"))
writeLines(hdr, file.path(dir, "ID002_K_KG40.asc"))
writeLines(hdr, file.path(dir, "ID002_K_KG40_R01.asc"))  # repeat take

lay <- pr_layout(
  2, 2, matrix(TRUE, 2, 2),
  data.frame(sensor_id = 1:4, row = c(1L, 2L, 1L, 2L),
             col = c(1L, 1L, 2L, 2L),
             x_mm = c(0, 0, 1, 1), y_mm = c(0, 1, 0, 1))
)
ds <- pr_read_dir(dir, layout = lay,
                  meta_fields = c("ID", "Saddle", "Weight"), quiet = TRUE)
length(ds)
attr(ds, "read_report")$status
unlink(dir, recursive = TRUE)
}
\seealso{
Other Pliance ingest functions:
\code{\link[=pr_meta_from_filename]{pr_meta_from_filename()}},
\code{\link[=pr_read_pliance]{pr_read_pliance()}}
}
\concept{Pliance ingest functions}
