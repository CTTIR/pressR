% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regional-symmetry.R
\name{pr_calc_symmetry_map}
\alias{pr_calc_symmetry_map}
\title{Left/Right Asymmetry of the Time-Averaged Sensor Map}
\usage{
pr_calc_symmetry_map(
  trial,
  masks = NULL,
  statistic = "mean",
  denominator = c("grid", "loaded")
)
}
\arguments{
\item{trial}{A \link{pr_trial} object.}

\item{masks}{What to compare. \code{NULL} (default) uses the whole active grid,
split at \code{floor(grid_cols / 2)} exactly as \code{\link[=pr_mask_symmetry]{pr_mask_symmetry()}} does. A
single \link{pr_mask} or logical matrix restricts that comparison to a region
of interest, split at the same midline. A list of exactly two masks
gives the two sides explicitly, left first.}

\item{statistic}{Character. \code{"mean"} (default), \code{"max"} or \code{"loaded"};
see \code{\link[=pr_calc_regional_map]{pr_calc_regional_map()}}.}

\item{denominator}{Character. \code{"grid"} (default) or \code{"loaded"}; see
\emph{Details}.}
}
\value{
A one-row \link[tibble:tibble]{tibble::tibble} with columns \code{statistic},
\code{denominator}, \code{n_left}, \code{n_right}, \code{left_value}, \code{right_value} and
\code{asymmetry_pct}.
}
\description{
The classic asymmetry index \code{(L - R) / (0.5 * (L + R)) * 100}, computed on
the per-sensor map rather than frame by frame. Negative values are
right-biased, positive values left-biased, and zero is symmetric.
}
\details{
\code{L} and \code{R} are the mean of the per-sensor map over the left and right
sensors of the region of interest. Because the map is taken first, the
index describes where load sat \emph{over the whole recording}, not where the
loudest frame happened to be; it is the counterpart of the frozen
\code{\link[=pr_calc_symmetry_index]{pr_calc_symmetry_index()}}, which averages within frames.

\code{denominator} chooses what goes into each side's mean:
\itemize{
\item \code{"grid"} (default) averages over \strong{every} sensor of the side, counting
never-loaded ones as zeros. This is the cohort definition: on a mat that
\emph{is} the region of interest, an unloaded cell is a real measurement.
\item \code{"loaded"} averages only the sensors whose map value is above zero. That
answers a different question — how hard the loaded cells were pressed,
ignoring how many there were — and is usually much closer to zero,
because a side that loses contact area keeps its intensity.
}

The index is undefined when both sides are zero; that case returns \code{0},
as \code{\link[=pr_calc_symmetry_index]{pr_calc_symmetry_index()}} does.

Nothing here forces the two sides to hold the same number of sensors. When
they do not, part of the index is a property of the mask — see
\code{\link[=pr_mask_mirror_balance]{pr_mask_mirror_balance()}} and \code{\link[=pr_symmetry_sensitivity]{pr_symmetry_sensitivity()}}.
}
\examples{
trial <- pr_example_trial("saddle_horse")
pr_calc_symmetry_map(trial)

# Restricted to the cranial band only:
bands <- pr_mask_rowbands(trial$layout, sides = FALSE)
pr_calc_symmetry_map(trial, bands$cranial)

# Intensity-only comparison, ignoring how much area each side loaded:
pr_calc_symmetry_map(trial, denominator = "loaded")$asymmetry_pct
}
\seealso{
\code{\link[=pr_calc_symmetry_index]{pr_calc_symmetry_index()}} for the frame-first version,
\code{\link[=pr_symmetry_sensitivity]{pr_symmetry_sensitivity()}} to vary the mask.

Other regional symmetry functions:
\code{\link[=pr_calc_cop_masked]{pr_calc_cop_masked()}},
\code{\link[=pr_calc_regional_map]{pr_calc_regional_map()}},
\code{\link[=pr_mask_mirror_balance]{pr_mask_mirror_balance()}},
\code{\link[=pr_mask_rowbands]{pr_mask_rowbands()}},
\code{\link[=pr_symmetry_sensitivity]{pr_symmetry_sensitivity()}}
}
\concept{regional symmetry functions}
