% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{pr_calc_spectrum}
\alias{pr_calc_spectrum}
\title{Power Spectrum of a Frame-Level Signal}
\usage{
pr_calc_spectrum(
  trial,
  signal = c("total", "mean", "peak"),
  window = c("hann", "none"),
  detrend = TRUE
)
}
\arguments{
\item{trial}{A \link{pr_trial} object.}

\item{signal}{Character. Which frame-level signal to transform: \code{"total"}
(default), \code{"mean"} or \code{"peak"}. See \emph{Details}.}

\item{window}{Character. \code{"hann"} (default) or \code{"none"}.}

\item{detrend}{Logical. Subtract the signal mean first. Default \code{TRUE}.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with \code{n \%/\% 2 + 1} rows and columns \code{freq_Hz}
(ascending, starting at 0) and \code{power}.
}
\description{
Frequency content of a whole-mat time series: how much of the recording's
variation sits at each frequency. On a saddle mat the stride shows up as
a peak somewhere between 0.5 and 2 Hz.
}
\details{
The signal is taken from the pressure matrix directly:
\itemize{
\item \code{"total"} — the frame sum, \code{\link[=pr_calc_total_pressure]{pr_calc_total_pressure()}}.
\item \code{"mean"} — the whole-grid frame mean, \code{\link[=pr_calc_mean_pressure_grid]{pr_calc_mean_pressure_grid()}}.
This is \code{"total"} divided by the constant \code{n_sensors}, so its spectrum
is the \code{"total"} spectrum scaled by \code{1 / n_sensors^2} and the peak sits
at exactly the same frequency.
\item \code{"peak"} — the frame maximum, which is \emph{not} a scaled copy of the other
two and saturates flat on a device that clips at its ceiling.
}

\code{detrend = TRUE} subtracts the mean before transforming, which removes
the DC term that would otherwise dominate a signal sitting on a large
constant offset. It does not remove a linear ramp. \code{window = "hann"}
then tapers the ends, trading a little frequency resolution for far less
leakage from the strong low-frequency components a drifting baseline
produces; \code{window = "none"} leaves the segment rectangular.

Power is \code{Mod(fft(x))^2 / n}, kept for the non-negative frequencies only
(\code{n \%/\% 2 + 1} values, including DC and — for even \code{n} — Nyquist). It is
a squared pressure quantity in the device's own unit, not a density: it
is not divided by the frequency bin width, and it is not corrected for
the window's power loss, so absolute values are comparable only between
recordings of the same length analysed the same way.
}
\section{Frequency axis}{

The axis is built as \code{(0:(n \%/\% 2)) * fs / n}, which is the exact centre
frequency of FFT bin \code{k} for both odd and even \code{n}.

The study this function reproduces built it as
\code{seq(0, fs / 2, length.out = length(power))} instead. For even \code{n} the
two agree exactly. For odd \code{n} they do not: that sequence has spacing
\code{fs / (n - 1)} rather than \code{fs / n}, and it places its last value at
\code{fs / 2}, which for odd \code{n} is not an FFT bin at all — the highest real
bin is at \code{((n - 1) / 2) * fs / n}, just below Nyquist. On the 869-frame
reference recording the error is a uniform stretch of \code{n / (n - 1)},
about 0.12\%: the study reports a stride at 0.7488 Hz where the exact
axis puts it at 0.7480 Hz. \code{power} is unaffected — only the labels move.
\strong{This function implements the exact axis.}
}

\examples{
trial <- pr_example_trial("saddle_horse")
spec <- pr_calc_spectrum(trial)
nrow(spec) == trial$n_frames \%/\% 2 + 1

# The strongest non-DC component is the stride:
round(spec$freq_Hz[which.max(spec$power[-1]) + 1], 3)

# "mean" is "total" scaled by n_sensors, so power scales by its square:
m <- pr_calc_spectrum(trial, signal = "mean")
all.equal(m$power * trial$layout$n_sensors^2, spec$power)
}
\seealso{
\code{\link[=pr_calc_dominant_freq]{pr_calc_dominant_freq()}} for the peak of this spectrum,
\code{\link[=pr_calc_stride_cycles]{pr_calc_stride_cycles()}} for the time-domain counterpart.

Other temporal structure functions:
\code{\link[=pr_calc_dominant_freq]{pr_calc_dominant_freq()}},
\code{\link[=pr_calc_phase_map]{pr_calc_phase_map()}},
\code{\link[=pr_calc_stride_cycles]{pr_calc_stride_cycles()}},
\code{\link[=pr_cop_shape]{pr_cop_shape()}}
}
\concept{temporal structure functions}
