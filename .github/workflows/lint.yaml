on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

name: lint

permissions: read-all

jobs:
  lint:
    uses: CTTIR/.github/.github/workflows/lint.yaml@main
    with:
      # FROZEN package - it underpins manuscripts in press. Every quality
      # gate is report-only here; only api-lock blocks.
      blocking: false
